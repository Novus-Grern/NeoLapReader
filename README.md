# NeoLapReader
A python file that takes a csv of lap times and displays them as a time graph

## What it do?
takes a csv or log file from an assetto corsa plugin (laplogger) and can display a time graph along with other neat things

## How it do?
Takes an text file generated by a certain lap logging plugin, processes it using matplotlib and numpy and displays it using tkinter and matplotlib. simple.

## What do next?
* Make it so it can ingest data in real time?
* Point it to look at a folder for any new files and automatically display it?
* A webapp like some server hosting sites used except its probably just a graph that updates with each lap?
* A way to display the car + track combo
* Move session Statistics Panel to main Window to make understanding what is in the text file easier

## What is here?
* Neolapreader.py - My python app to read and dislplay logs
* laplogger.py - the laplogger i found on racedepartment that i thought could use improvement